import express from "express";

const router = express.Router();

/**
 * Register
 * username -> string
 * email -> string
 * password -> string
 * confirmPassword -> string
 */
router.post("/register", )